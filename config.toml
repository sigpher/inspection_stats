month = "08"

ocr_lang = "chi_sim+eng"

# 本地 Umi-OCR 开放 API 地址（需启用“开放API接口服务”）。可配置多个实例做负载均衡，
# 程序会并发分配到不同实例，真正并行提速。每个实例对应一个独立的 Umi-OCR 进程/端口。
umi_ocr_urls = [
  "http://127.0.0.1:1224",
  "http://127.0.0.1:1225",
  "http://127.0.0.1:1226",
]

ocr_dpi = 150  # OCR 光栅化分辨率：越低越快（精度略降），可在 120~200 间调整

search = ["天地壹号","天晨","巴马世界"]
