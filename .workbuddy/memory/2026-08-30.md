# 2026-08-30

## 刷新 AGENTS.md（/init）
通读全部 10 个源文件 + config.toml / website.md / logs 后重写 `AGENTS.md`，修正原版与代码不符之处：

- **下载文件名实为 `地区-期号-附件原标题.ext`**（如 `江门-第8期-附件1：食品抽检信息汇总表.pdf`），旧文档写的 `地区-第xx期.ext` 是错的；去重后缀是 `-标题-2.ext` 而非 `地区-期-2.ext`。
- 期号回退链：标题「第N期/号/批」→ 标题内 8 位日期 → 发布日期 `YYYYMMDD` → `期数未知`。
- `unparsed` 表只记录解析失败的文件，不是「Mixed」——`Kind::Mixed` 是下载期的分类（合格+不合格合并表），别混用。
- 地区→爬取函数映射：江西 `crawl_jiangxi`（POST /queryList，≤10 页，遇早于目标月即停）；湖南/福建 `crawl_datepath`（URL 含 `YYYYMM` 即命中，**不校验发布日期**）；其余 10 个 `crawl_trs`（候选链接上限 60 条，再用发布日期过滤）。
- HTTP：4xx 立即失败不重试，5xx 才重试；每次重试内先 https 再降级 http。

顺带修 README 三处过期描述（文件名格式、`logs/{year}-{month}.log` 路径、.gitignore 声称忽略 config.toml 但实为已跟踪）。

## 值得记的两个代码缺陷（未改，仅记录）
- `result.db` 的 `run_at` 用 `SystemTime` 直算 = **UTC**，而 `logs/` 时间戳走 `tz_offset` = UTC+8，两者差 8 小时。
- `search.rs::pdf_needs_ocr` 的注释写「30s 超时」，实际是 `thread::scope` 直接 join，**没有超时**，畸形 PDF 可能挂死整个检索。

## 环境状态
- `cargo check` 通过（25s，无 warning）。仓库无下载目录、无 `result.db`，需 `cargo run` 现跑。
- 湖北每次都 HTTP 412 WAF → 0 文件；广州/佛山/广西常 0 文件。
